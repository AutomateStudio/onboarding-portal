import { OnboardingLayout } from '@/components/OnboardingLayout';

export const metadata = {
  title: 'Configura tu Tienda Shopify | Automate Studio',
  description: 'Configura tu tienda Shopify profesional en minutos. Elige tu industria, template y plan. Sin código. Sin complicaciones.',
};

export default function OnboardingPage() {
  return <OnboardingLayout />;
}
